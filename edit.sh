#!/bin/bash

uvx marimo edit --headless --no-token --port 8080
# Sandbox mode not available for editting multiple files, so does not work for a single edit script where you can have the choice to edit multiple files.
